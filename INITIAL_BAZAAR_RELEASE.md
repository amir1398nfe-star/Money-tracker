# نسخه اولیه انتشار در بازار

این نسخه برای انتشار اولیه در کافه‌بازار بدون پرداخت درون‌برنامه‌ای آماده شده است. نمودار، گزارش‌ها و بخش چک‌ها در این build بدون پرداخت باز هستند. این حالت با `INITIAL_FREE_BUILD = true` در `lib/finance-store.tsx` کنترل می‌شود.

## انتشار

برای انتشار، یک build اندروید با `applicationId` فعلی پروژه (`com.app.pocketbudget`) بسازید و آن را در پنل توسعه‌دهندگان بازار، ابتدا در کانال آزمایشی و سپس در انتشار عمومی آپلود کنید. بعد از تأیید نصب و عملکرد نسخه رایگان، برای نسخه پولی مقدار `INITIAL_FREE_BUILD` به `false` تغییر می‌کند، محصول `premium_pie_chart` در بازار ساخته می‌شود و کلید RSA همان برنامه به تنظیمات امن پروژه اضافه می‌گردد.

پیش‌نمایش وب و Expo Go جایگزین تست APK بازار نیستند؛ یادآورها، اعلان‌ها و عملکرد native باید روی APK نصب‌شده آزمایش شوند.
