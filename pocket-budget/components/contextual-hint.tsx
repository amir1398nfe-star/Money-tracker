import React, { useEffect, useState } from "react";
import AsyncStorage from "@react-native-async-storage/async-storage";
import { Pressable, Text, View } from "react-native";
import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { localeMeta, t, type Locale, type TranslationKey } from "@/lib/i18n";

export function ContextualHint({ screen, locale, titleKey, bodyKey, icon }: { screen: string; locale: Locale; titleKey: TranslationKey; bodyKey: TranslationKey; icon: React.ComponentProps<typeof MaterialIcons>["name"] }) { const [visible, setVisible] = useState(false); const rtl = localeMeta[locale].direction === "rtl"; useEffect(() => { AsyncStorage.getItem(`pocket-budget-hint-${screen}-seen-v1`).then((value) => setVisible(value !== "1")).catch(() => setVisible(true)); }, [screen]); if (!visible) return null; const dismiss = () => { setVisible(false); void AsyncStorage.setItem(`pocket-budget-hint-${screen}-seen-v1`, "1"); }; return <View style={{ marginTop: 14, backgroundColor: "#F0ECFF", borderRadius: 18, padding: 14, flexDirection: rtl ? "row-reverse" : "row", alignItems: "flex-start", gap: 10 }}><View style={{ width: 32, height: 32, borderRadius: 11, backgroundColor: "#FFFFFF", alignItems: "center", justifyContent: "center" }}><MaterialIcons name={icon} size={17} color="#7C5CFC" /></View><View style={{ flex: 1, gap: 4, alignItems: rtl ? "flex-end" : "flex-start" }}><Text style={{ color: "#4E3C96", fontSize: 12, fontWeight: "900", textAlign: rtl ? "right" : "left" }}>{t(locale, titleKey)}</Text><Text style={{ color: "#7767B0", fontSize: 11, lineHeight: 17, textAlign: rtl ? "right" : "left" }}>{t(locale, bodyKey)}</Text></View><Pressable onPress={dismiss}><Text style={{ color: "#6C4EE4", fontSize: 10, fontWeight: "900" }}>{t(locale, "hintGotIt")}</Text></Pressable></View>; }
